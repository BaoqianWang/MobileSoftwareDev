<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:background="@color/colorSelected1"
    tools:context=".MainActivity">

    <!--<Button
        android:id="@+id/academicButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="launchAcademicActivity"
        android:background="@drawable/bg_calendar"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:text="@string/academic_calendar" />

    <Button
        android:id="@+id/personalButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="launchPersonalActivity"
        android:background="@drawable/bg_calendar"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:text="@string/personal_calendar" />

    <Button
        android:id="@+id/journalButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="launchJournalActivity"
        android:background="@drawable/bg_calendar"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:text="@string/journal" />
-->

</LinearLayout>