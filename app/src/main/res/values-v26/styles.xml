<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorSelected2</item>
        <item name="android:textColor">@color/colorSelected2</item>
        <item name="android:textColorPrimary">@color/colorSelected2</item>
        <item name="android:datePickerDialogTheme">@style/MyDatePickerDialogTheme</item>
        <item name="android:calendarTextColor">@color/colorSelected1</item>
        <item name="android:actionMenuTextColor">@color/colorSelected2</item>
        <item name="android:itemBackground">@color/colorSelected2</item>
        <item name="android:itemTextAppearance">@color/colorSelected2</item>
        <item name="android:iconTintMode">@color/colorSelected2</item>

        <item name="android:editTextBackground">@color/colorSelected2</item>
    </style>
</resources>